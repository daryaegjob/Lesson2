/**
 * Одна запись (строка) в таблице.
 * Значения хранятся строго в том же порядке, что и columnNames таблицы.
 *
 * Обратите внимание: record с полем-массивом не даёт "честных" equals/hashCode
 * (массивы сравниваются по ссылке), это осознанное упрощение для учебной задачи.
 */
public record Row(String[] columns) {
}
